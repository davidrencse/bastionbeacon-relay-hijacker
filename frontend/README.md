# BastionBeacon Relay Hijacker Frontend

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if your backend is not running on `http://localhost:3000`.

## Build

```bash
npm run build
```

The Vite base path is configured for GitHub Pages at `/bastionbeacon-relay-hijacker/`.
